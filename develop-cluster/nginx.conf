server {
        listen 80 default_server;
        listen [::]:80 default_server;

        #root /var/www/html;

        index index.html;

        server_name tracker;

        location / {
            root /var/www/html;

        }

}